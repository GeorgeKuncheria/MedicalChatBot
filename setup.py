from setuptools import setup, find_packages

setup(
    name='medical_chatbot',
    version='0.0.0',
    author='George Kuncheria',
    author_email='georgekuncheria904@gmail.com',
    packages=find_packages(),
    install_requires=[]
)

#use this setup.py to install the package with pip install -r requirements.txt.
# In requirements.txt, add ".e -"